{% extends extends_base ? '@App/base.html.twig' : 'clean.html.twig' %}

{% block body %}
    {% if not app.request.xmlHttpRequest %}
        <ul class="posts">
    {% endif %}

        {% for post in posts %}
            {% include '@App/Post/_post.html.twig' %}
        {% endfor %}

     {% if not app.request.xmlHttpRequest %}
         </ul>
     {% endif %}
{% endblock %}

