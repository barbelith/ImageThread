{# @var post \AppBundle\Entity\Post #}
<li class="post">
    {% if post.title %}
    <h2>{{ post.title }}</h2>
    {% endif %}

    <img src="{{ post.image|image_url }}" class="u-max-full-width"/>
</li>