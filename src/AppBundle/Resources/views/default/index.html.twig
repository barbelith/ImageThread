{% extends '@App/base.html.twig' %}

{% block body %}
    <div class="row">
        {{ render(controller('AppBundle:Post:create')) }}
    </div>

    <div class="row">
        {{ render(controller('AppBundle:Post:list')) }}
    </div>
{% endblock %}